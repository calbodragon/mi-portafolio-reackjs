import React from 'react';
import ReactDOM from 'react-dom';
import About from './components/About';
// que voy a renderixar (el componente)
ReactDOM.render(<About/>,document.getElementById('app'));
//donde lo voy  arenderizar
